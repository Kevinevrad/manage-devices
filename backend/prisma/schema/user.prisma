
model User {
    id        Int     @id @default(autoincrement())
    nom       String
    prenom    String
    email     String  @unique
    structure String 
    service   String
    role      String  @default("user") // "user", "admin", "technicien"

    equipements Equipement[]
}