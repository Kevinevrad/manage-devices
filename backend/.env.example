# Copier ce fichier en .env puis adapter les valeurs

# Connexion à la base SQLite (chemin relatif à la racine du backend)
DATABASE_URL="file:./prisma/manage_devices.db"

# Port d'écoute de l'API
PORT=3000