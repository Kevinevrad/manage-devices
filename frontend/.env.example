# Copier ce fichier en .env puis adapter les valeurs

# URL de l'API backend.
# En développement, laisser vide : le proxy Vite relaie /api vers
# http://localhost:3000 (voir vite.config.ts).
# En production, pointer vers l'URL publique de l'API, par exemple :
# VITE_API_URL="https://api.mon-domaine.fr/api"
